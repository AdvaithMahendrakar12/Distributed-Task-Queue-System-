global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: task-queue
    metrics_path: /metrics
    static_configs:
      - targets: ["host.docker.internal:4000"]
